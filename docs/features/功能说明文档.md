# "我的"页面功能文档

## 1. 功能概述

"我的"页面是 K线训练营应用的用户个人中心，提供用户信息管理、训练数据统计、功能导航等核心功能。

---

## 2. 功能模块

### 2.1. 用户信息卡片

**位置**: 页面顶部

**功能描述**:
- 展示用户头像（圆形占位符，支持点击更换）
- 展示用户昵称（支持点击修改）
- 展示脱敏手机号（格式：138****8001）
- 展示会员等级徽章

**交互设计**:
| 操作 | 触发方式 | 反馈 |
|------|----------|------|
| 更换头像 | 点击头像 | 弹出底部选择菜单（相册/拍照） |
| 修改昵称 | 点击昵称旁编辑图标 | 弹出修改对话框 |

### 2.2. 训练统计卡片

**位置**: 用户信息卡片下方

**功能描述**:
- 训练次数：展示用户累计训练天数
- 总收益率：展示累计盈亏百分比（红色为正，绿色为负）
- 交易胜率：展示交易获胜比例
- 学习进度：展示课程完成百分比

**数据来源**:
- 从数据库 `users` 表读取 `totalTrainingDays`、`totalProfit`、`overallWinRate` 等字段

### 2.3. 功能菜单区

**位置**: 统计卡片下方

**菜单列表**:

| 菜单名称 | 图标 | 目标页面 | 说明 |
|----------|------|----------|------|
| 自选管理 | ⭐ | /mine/favorites | 管理关注品种 |
| 训练记录 | 📊 | /mine/training-history | 查看历史训练 |
| 学习进度 | 📚 | /mine/learning-progress | 课程学习进度 |
| 消息通知 | 🔔 | /mine/notifications | 系统通知（带未读数） |
| 意见反馈 | 💬 | /mine/feedback | 用户反馈提交 |
| 帮助中心 | ❓ | /mine/help-center | 常见问题解答 |
| 关于我们 | ℹ️ | 弹窗 | 应用信息 |

### 2.4. 设置入口

**位置**: 用户信息卡片右上角

**功能描述**:
- 点击齿轮图标进入设置页面
- 设置页面包含显示设置、交易设置等

### 2.5. 退出登录

**位置**: 页面底部

**功能描述**:
- 显示"退出登录"按钮
- 点击后弹出确认对话框
- 确认后清除登录状态，跳转登录页面

---

## 3. 用户认证流程

### 3.1. 注册流程

```
注册页面 → 输入手机号 → 获取验证码 → 输入验证码 → 输入密码 → 点击注册 → 保存数据库 → 跳转我的页面
```

**验证规则**:
- 手机号必须为11位数字
- 验证码必须为6位数字
- 密码至少6位

### 3.2. 登录流程

```
登录页面 → 输入手机号 → 输入密码 → 点击登录 → 验证数据库 → 跳转我的页面
```

---

## 4. 数据流向

```
注册/登录 → AuthProvider.register/login() → UserRepository.saveUserToDatabase()
                                                    ↓
                                           SQLite数据库(users表)
                                                    ↓
                                  UserRepository.getCurrentUser() → UserProvider
                                                    ↓
                                               MineScreen展示
```

---

## 5. 功能状态

| 功能模块 | 状态 | 备注 |
|----------|------|------|
| 用户信息展示 | ✅ 已实现 | 含头像、昵称、等级徽章 |
| 训练统计展示 | ✅ 已实现 | 含训练次数、收益率、胜率、进度 |
| 功能菜单 | ✅ 已实现 | 7个功能入口 |
| 设置入口 | ✅ 已实现 | 跳转设置页面 |
| 退出登录 | ✅ 已实现 | 带确认对话框 |
| 用户注册 | ✅ 已实现 | 手机号+验证码+密码 |
| 用户登录 | ✅ 已实现 | 手机号+密码 |
| 数据持久化 | ✅ 已实现 | SQLite本地存储 |

---

**文档版本**: v1.0  
**创建日期**: 2026-05-14  
**最后更新**: 2026-05-14