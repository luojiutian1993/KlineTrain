# 首页选股条件模块重构技术设计文档

## 概述

本文档描述 KlineTrain 项目首页选股条件模块重构的技术实现方案，包括市场选择层次、随机时间点生成、训练周期联动、选股条件重定义等功能。

## 功能需求

| 需求编号 | 功能描述 | 优先级 | 实现状态 |
|:---:|------|:---:|:---:|
| REQ-HOME-014 | 市场选择层次重构：一级分类下拉框（A股/港股/美股/期货），默认A股 | 高 | ⏳ 待实现 |
| REQ-HOME-015 | A股子板块多选：选择A股后，支持多选上证、深证等子板块 | 高 | ⏳ 待实现 |
| REQ-HOME-016 | 行业板块选择预留：新增行业板块下拉框占位（后续实现） | 中 | ⏳ 待实现 |
| REQ-HOME-017 | 删除时间周期选择：移除原有的时间范围选择功能 | 高 | ⏳ 待实现 |
| REQ-HOME-018 | 随机时间点生成：在数据库时间范围内随机生成训练起始时间点 | 高 | ⏳ 待实现 |
| REQ-HOME-019 | 训练周期固定150天：训练周期固定为150天，与实战页面联动 | 高 | ⏳ 待实现 |
| REQ-HOME-020 | 时间文本框显示：显示随机生成的时间点，当前版本始终显示 | 高 | ⏳ 待实现 |
| REQ-HOME-021 | 选股条件重定义：18个选股条件计算周期为数据库最早日期到随机时间点 | 高 | ⏳ 待实现 |

---

## 1. 市场选择层次设计

### 1.1 市场分类结构

```
市场选择层次
├── 一级分类（下拉框）
│   ├── A股（默认）
│   ├── 港股
│   ├── 美股
│   └── 期货
│
└── 二级子板块（多选，仅选择A股时显示）
    ├── 上证（SH）
    ├── 深证（SZ）
    └── 上交所（XSHG）
```

### 1.2 市场代码映射

| 一级分类 | 代码 | 说明 |
|---------|------|------|
| A股 | CN | 中国大陆A股市场 |
| 港股 | HK | 香港交易所 |
| 美股 | US | 美国市场 |
| 期货 | FUT | 期货市场 |

| 二级子板块（A股） | 前端代码 | 数据库代码 |
|------------------|---------|-----------|
| 上证 | XSHG | SH |
| 深证 | XSHE | SZ |
| 上交所 | SSE | SH |

### 1.3 UI组件设计

**文件位置**：`lib/features/home/widgets/market_selector.dart`

```dart
class MarketSelector extends StatelessWidget {
  final String selectedMarket;
  final List<String> selectedSubMarkets;
  final ValueChanged<String> onMarketChanged;
  final ValueChanged<List<String>> onSubMarketsChanged;
  
  // 一级市场选项
  static const marketOptions = [
    {'code': 'CN', 'label': 'A股', 'icon': '🇨🇳'},
    {'code': 'HK', 'label': '港股', 'icon': '🇭🇰'},
    {'code': 'US', 'label': '美股', 'icon': '🇺🇸'},
    {'code': 'FUT', 'label': '期货', 'icon': '📈'},
  ];
  
  // A股子板块选项
  static const subMarketOptions = [
    {'code': 'XSHG', 'label': '上证'},
    {'code': 'XSHE', 'label': '深证'},
    {'code': 'SSE', 'label': '上交所'},
  ];
}
```

### 1.4 状态管理

**文件位置**：`lib/providers/selection_provider.dart`

```dart
class SelectionState {
  // 新增字段
  final String selectedMarket;           // 一级市场 CN/HK/US/FUT
  final List<String> selectedSubMarkets; // 二级子板块列表
  
  // 移除字段
  // final StockTimeRange timeRange;      // 删除时间范围选择
}
```

---

## 2. 随机时间点生成设计

### 2.1 时间范围获取

**文件位置**：`lib/data/database/daos/stock_filter_dao.dart`

```dart
/// 获取K线数据库的时间范围
Future<(DateTime, DateTime)> getKlineDateRange() async {
  final minResult = await (selectOnly(klineData)
        ..addColumns([klineData.tradeDate.min()]))
      .getSingle();
  
  final maxResult = await (selectOnly(klineData)
        ..addColumns([klineData.tradeDate.max()]))
      .getSingle();
  
  final minTimestamp = minResult.read(klineData.tradeDate.min());
  final maxTimestamp = maxResult.read(klineData.tradeDate.max());
  
  return (
    DateTime.fromMillisecondsSinceEpoch(minTimestamp! * 1000),
    DateTime.fromMillisecondsSinceEpoch(maxTimestamp! * 1000),
  );
}
```

### 2.2 随机时间点生成服务

**文件位置**：`lib/data/services/time_range_service.dart`

```dart
class TimeRangeService {
  final StockFilterDao _stockFilterDao;
  
  /// 在数据库时间范围内随机生成一个时间点
  /// 预留150天训练周期的余量
  Future<DateTime> generateRandomTrainingStartDate({
    int trainingDays = 150,
  }) async {
    final (minDate, maxDate) = await _stockFilterDao.getKlineDateRange();
    
    // 计算最早可开始日期（留出训练周期的余量）
    final earliestStartDate = minDate.add(Duration(days: 300)); // 额外留出300天用于指标计算
    
    // 计算最晚可开始日期（留出训练周期）
    final latestStartDate = maxDate.subtract(Duration(days: trainingDays));
    
    if (earliestStartDate.isAfter(latestStartDate)) {
      throw Exception('数据库数据不足，无法生成有效的训练起始日期');
    }
    
    // 生成随机日期
    final random = Random();
    final daysBetween = latestStartDate.difference(earliestStartDate).inDays;
    final randomDays = random.nextInt(daysBetween + 1);
    
    return earliestStartDate.add(Duration(days: randomDays));
  }
}
```

### 2.3 时间文本框组件

**文件位置**：`lib/features/home/widgets/training_date_display.dart`

```dart
class TrainingDateDisplay extends StatelessWidget {
  final DateTime? startDate;
  final int trainingDays;
  final VoidCallback onRegenerate;
  
  @override
  Widget build(BuildContext context) {
    final endDate = startDate?.add(Duration(days: trainingDays));
    
    return Card(
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                const Text('训练时间范围'),
                TextButton(
                  onPressed: onRegenerate,
                  child: const Text('重新生成'),
                ),
              ],
            ),
            const SizedBox(height: 8),
            Text(
              startDate != null 
                ? '训练起始时间：${_formatDate(startDate!)}'
                : '加载中...',
              style: const TextStyle(fontWeight: FontWeight.bold),
            ),
            Text(
              endDate != null
                ? '训练结束时间：${_formatDate(endDate!)}'
                : '',
            ),
            Text('训练周期：$trainingDays天'),
          ],
        ),
      ),
    );
  }
  
  String _formatDate(DateTime date) {
    return '${date.year}-${date.month.toString().padLeft(2, '0')}-${date.day.toString().padLeft(2, '0')}';
  }
}
```

---

## 3. 选股条件重定义

### 3.1 选股条件计算周期变更

**核心变更**：所有选股条件的计算周期从"固定日期"改为"数据库最早日期到随机时间点"

**文件位置**：`lib/data/repositories/stock_filter_repository.dart`

```dart
Future<StockFilterResultResponse> filterStocks({
  required StockFilterCondition condition,
  required DateTime trainingStartDate,  // 新增：训练起始时间点
  String? marketCode,
  List<String>? subMarketCodes,        // 新增：子板块代码列表
  int trainingDays = 150,
}) async {
  // 获取数据库最早日期
  final (minDate, _) = await _stockFilterDao.getKlineDateRange();
  
  appLogger.i(
    '开始选股: condition=${condition.name}, '
    'startDate=$minDate, endDate=$trainingStartDate, '
    'trainingDays=$trainingDays'
  );
  
  // 根据选股条件筛选股票
  final symbols = await _stockFilterDao.filterByCondition(
    condition: condition,
    startDate: minDate,       // 数据库最早日期
    endDate: trainingStartDate, // 随机生成的训练起始时间点
    marketCodes: subMarketCodes,
  );
  
  // 获取股票详情
  final results = await _getStockDetails(symbols, trainingStartDate);
  
  return StockFilterResultResponse(
    items: results,
    total: results.length,
    trainingStartDate: trainingStartDate,
    trainingDays: trainingDays,
  );
}
```

### 3.2 选股条件枚举更新

**文件位置**：`lib/core/enums/stock_filter_condition.dart`

```dart
enum StockFilterCondition {
  // 新高类
  allTimeHigh('历史新高'),
  yearHigh('1年新高'),
  day200High('200日新高'),
  
  // 新低类
  allTimeLow('历史新低'),
  yearLow('1年新低'),
  day200Low('200日新低'),
  
  // 涨跌幅排名类
  gain30dTop('30日涨幅前50%'),
  gain15dTop('15日涨幅前50%'),
  loss30dTop('30日跌幅前50%'),
  loss15dTop('15日跌幅前50%'),
  
  // 涨停跌停类
  limitUp('涨停'),
  limitDown('跌停'),
  consecutiveLimitUp('连板'),
  consecutiveLimitDown('连续跌停'),
  
  // 量价类
  volumePriceUp('量升价涨'),
  
  // 趋势类
  upTrend('上升趋势'),
  downTrend('下降趋势'),
}
```

---

## 4. 实战页面联动设计

### 4.1 传递参数增强

**文件位置**：`lib/features/home/home_screen.dart`

```dart
// 确认按钮逻辑
void _onConfirm() {
  final selectedStock = ref.watch(stockFilterProvider).selectedStock;
  final trainingStartDate = ref.watch(selectionProvider).trainingStartDate;
  final trainingDays = 150;
  
  if (selectedStock == null) {
    ScaffoldMessenger.of(context).showSnackBar(
      const SnackBar(content: Text('请先选择一只股票'))
    );
    return;
  }
  
  Navigator.push(
    context,
    MaterialPageRoute(
      builder: (context) => BattleScreen(
        stockCode: selectedStock.symbol,
        stockName: selectedStock.name,
        marketCode: selectedStock.marketCode,
        trainingStartDate: trainingStartDate,
        trainingDays: trainingDays,
      ),
    ),
  );
}
```

### 4.2 实战页面接收参数

**文件位置**：`lib/features/battle/battle_screen.dart`

```dart
class BattleScreen extends StatefulWidget {
  final String? stockCode;
  final String? stockName;
  final String? marketCode;
  final DateTime? trainingStartDate;  // 新增：训练起始时间点
  final int trainingDays;             // 新增：训练周期（默认150天）
  
  const BattleScreen({
    super.key,
    this.stockCode,
    this.stockName,
    this.marketCode,
    this.trainingStartDate,
    this.trainingDays = 150,
  });
}

@override
void initState() {
  super.initState();
  
  // 使用传递的训练起始日期和周期
  _trainingStartDate = widget.trainingStartDate ?? DateTime.now();
  _trainingDays = widget.trainingDays;
  
  if (widget.stockCode != null) {
    _loadKlineData(widget.stockCode!);
  }
}
```

### 4.3 K线数据加载逻辑

```dart
Future<void> _loadKlineData(String symbol) async {
  // 计算需要加载的数据范围
  // 历史缓冲：30天（用于指标计算）
  // 训练周期：150天
  const historyDays = 30;
  final totalDays = _trainingDays + historyDays;
  
  final data = await _klineRepository.fetchKlineData(
    symbol: symbol,
    startTime: _trainingStartDate?.subtract(Duration(days: historyDays)),
    endTime: _trainingStartDate?.add(Duration(days: _trainingDays)),
  );
  
  setState(() {
    _allKlineData = data;
    _currentDayIndex = historyDays; // 从历史缓冲结束处开始
    _tradePoints = [];
  });
}
```

---

## 5. 前后端及数据库联动

### 5.1 数据流向图

```
用户操作（首页）
        │
        ▼
┌─────────────────────────────────┐
│  MarketSelector                │
│  (选择市场和子板块)            │
└──────────┬────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  TimeRangeService              │
│  (生成随机时间点)              │
└──────────┬────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  StockFilterRepository         │
│  (执行选股逻辑)                │
└──────────┬────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  StockFilterDao                │
│  (数据库查询)                  │
└──────────┬────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  SQLite数据库                   │
│  (symbols + kline_data表)      │
└─────────────────────────────────┘
```

### 5.2 数据库查询流程

```sql
-- 1. 获取数据库时间范围
SELECT MIN(trade_date) as min_date, MAX(trade_date) as max_date 
FROM kline_data;

-- 2. 根据选股条件筛选股票
-- 示例：筛选历史新高股票
SELECT DISTINCT symbol 
FROM kline_data k1
WHERE k1.period = 'day'
  AND k1.trade_date = ?  -- 随机时间点
  AND k1.close = (
    SELECT MAX(close) 
    FROM kline_data k2 
    WHERE k2.symbol = k1.symbol 
      AND k2.period = 'day'
      AND k2.trade_date >= ?  -- 数据库最早日期
      AND k2.trade_date <= ?  -- 随机时间点
  );

-- 3. 获取股票基本信息
SELECT * FROM symbols WHERE symbol IN (...);
```

---

## 6. UI变更

### 6.1 首页选股区域结构

```dart
// 市场选择
MarketSelector(...)

// 行业板块选择（预留）
FutureBuilder(
  future: _getSectors(),
  builder: (context, snapshot) {
    if (snapshot.hasData) {
      return DropdownButtonFormField(
        items: snapshot.data!.map((sector) => 
          DropdownMenuItem(value: sector.code, child: Text(sector.name))
        ).toList(),
        value: null,
        hint: const Text('行业板块（后续实现）'),
        onChanged: null, // 暂不处理
        enabled: false,   // 禁用状态
      );
    }
    return const CircularProgressIndicator();
  },
)

// 时间显示（随机生成）
TrainingDateDisplay(
  startDate: _trainingStartDate,
  trainingDays: 150,
  onRegenerate: _regenerateDate,
)

// 选股条件（18个条件）
StockConditionSelector(...)

// 选股结果列表
StockResultList(...)

// 确认按钮
ElevatedButton(
  onPressed: _onConfirm,
  child: const Text('确认'),
)
```

---

## 7. 文件清单

### 7.1 修改的文件
| 文件 | 变更内容 |
|------|----------|
| `lib/features/home/home_screen.dart` | 更新UI结构，添加市场选择器和时间显示组件 |
| `lib/features/home/widgets/stock_condition_selector.dart` | 删除时间范围选择，更新选股条件调用 |
| `lib/providers/selection_provider.dart` | 添加市场选择状态，移除时间范围状态 |
| `lib/data/repositories/stock_filter_repository.dart` | 更新选股逻辑，使用新的时间范围参数 |
| `lib/data/database/daos/stock_filter_dao.dart` | 添加获取时间范围方法，更新选股查询 |
| `lib/features/battle/battle_screen.dart` | 接收训练起始日期和周期参数 |

### 7.2 新增的文件
| 文件 | 说明 |
|------|------|
| `lib/features/home/widgets/market_selector.dart` | 市场选择器组件 |
| `lib/features/home/widgets/training_date_display.dart` | 训练时间显示组件 |
| `lib/data/services/time_range_service.dart` | 时间范围服务 |
| `specs/首页选股条件模块重构技术设计文档.md` | 本文档 |

---

## 8. 测试验证

### 8.1 单元测试

```dart
void main() {
  group('TimeRangeService', () {
    test('generateRandomTrainingStartDate returns valid date', () async {
      final service = TimeRangeService(mockDao);
      final date = await service.generateRandomTrainingStartDate();
      
      expect(date, isNotNull);
      expect(date.isAfter(minDate), isTrue);
      expect(date.isBefore(maxDate.subtract(Duration(days: 150))), isTrue);
    });
  });
  
  group('StockFilterRepository', () {
    test('filterStocks uses correct date range', () async {
      final repo = StockFilterRepository(mockDao);
      final result = await repo.filterStocks(
        condition: StockFilterCondition.allTimeHigh,
        trainingStartDate: DateTime(2024, 1, 1),
        trainingDays: 150,
      );
      
      expect(result.trainingStartDate, equals(DateTime(2024, 1, 1)));
      expect(result.trainingDays, equals(150));
    });
  });
}
```

### 8.2 集成测试

| 测试场景 | 验证项 |
|---------|-------|
| 市场选择层次 | 一级分类切换后，二级子板块正确显示/隐藏 |
| 随机时间点生成 | 生成的时间点在数据库范围内，留出150天余量 |
| 时间重新生成 | 点击重新生成按钮后时间点改变 |
| 选股条件筛选 | 根据随机时间点正确筛选符合条件的股票 |
| 跳转实战页面 | 正确传递股票信息、训练起始日期和周期 |
| 实战页面显示 | K线从训练起始日期开始，显示150天训练周期 |

---

## 9. 训练时间生成失败修复设计

### 9.1 问题分析

**问题描述**：
- 数据库为空时，`getKlineDateRange()` 抛出异常
- `TimeRangeService` 缺少异常处理和默认值机制
- 应用可能崩溃或无响应

**根本原因**：
1. `selectOnly().getSingle()` 在空表时抛出异常
2. 缺少数据量前置检查
3. 没有降级方案作为兜底

### 9.2 StockFilterDao 增强设计

**文件位置**：`lib/data/database/daos/stock_filter_dao.dart`

**增强后的实现**：
```dart
Future<(DateTime, DateTime)> getKlineDateRange() async {
  appLogger.i('开始查询K线数据日期范围...');

  try {
    // 1. 先检查数据量
    final countResult = await (selectOnly(klineData)
          ..addColumns([klineData.symbol.count()])
          ..where(klineData.period.equals('day')))
        .getSingle();
    
    final count = countResult.read(klineData.symbol.count());
    if (count == null || count == 0) {
      appLogger.w('K线数据表为空，返回默认日期范围');
      return (DateTime(2000, 1, 1), DateTime.now());
    }

    // 2. 查询最小和最大日期
    final minResult = await (selectOnly(klineData)
          ..addColumns([klineData.tradeDate.min()])
          ..where(klineData.period.equals('day')))
        .getSingle();

    final maxResult = await (selectOnly(klineData)
          ..addColumns([klineData.tradeDate.max()])
          ..where(klineData.period.equals('day')))
        .getSingle();

    final minDate = minResult.read(klineData.tradeDate.min());
    final maxDate = maxResult.read(klineData.tradeDate.max());

    appLogger.i('查询到的日期范围: min=$minDate, max=$maxDate');

    return (
      minDate ?? DateTime(2000, 1, 1),
      maxDate ?? DateTime.now(),
    );
  } catch (e, stackTrace) {
    // 3. 异常处理
    appLogger.e('查询K线日期范围失败', error: e, stackTrace: stackTrace);
    return (DateTime(2000, 1, 1), DateTime.now());
  }
}
```

**增强点**：
- ✅ 数据量前置检查
- ✅ try-catch 异常捕获
- ✅ 空表返回默认日期范围
- ✅ 完善的日志记录

### 9.3 TimeRangeService 增强设计

**文件位置**：`lib/data/services/time_range_service.dart`

**增强后的实现**：
```dart
Future<DateTime> generateRandomTrainingStartDate({
  int trainingDays = 150,
}) async {
  appLogger.i('开始生成训练起始日期...');

  try {
    final (minDate, maxDate) = await _stockFilterDao.getKlineDateRange();
    appLogger.i('数据库日期范围: min=$minDate, max=$maxDate');

    final now = DateTime.now();
    
    // 1. 验证日期有效性
    if (minDate.isAfter(maxDate)) {
      appLogger.w('数据库日期范围无效(min > max)，使用默认日期范围');
      return _generateDefaultTrainingDate(trainingDays);
    }

    // 2. 检查数据量是否足够
    final dataRangeDays = maxDate.difference(minDate).inDays;
    if (dataRangeDays < trainingDays + 300) {
      appLogger.w('数据库数据不足，当前数据范围: $dataRangeDays天，需要至少${trainingDays + 300}天');
      
      // 2.1 如果数据量非常少，使用默认日期
      if (dataRangeDays < trainingDays) {
        return _generateDefaultTrainingDate(trainingDays);
      }
      
      // 2.2 如果有一些数据，尽量使用可用数据
      final adjustedEarliestStartDate = minDate;
      final adjustedLatestStartDate = maxDate.subtract(Duration(days: trainingDays));
      
      if (adjustedEarliestStartDate.isAfter(adjustedLatestStartDate)) {
        return _generateDefaultTrainingDate(trainingDays);
      }
      
      return _generateRandomDate(adjustedEarliestStartDate, adjustedLatestStartDate);
    }

    // 3. 正常流程
    final earliestStartDate = minDate.add(const Duration(days: 300));
    final latestStartDate = maxDate.subtract(Duration(days: trainingDays));
    appLogger.i('有效日期范围: start=$earliestStartDate, end=$latestStartDate');

    if (earliestStartDate.isAfter(latestStartDate)) {
      appLogger.w('计算后的日期范围无效，使用默认日期');
      return _generateDefaultTrainingDate(trainingDays);
    }

    return _generateRandomDate(earliestStartDate, latestStartDate);
    
  } catch (e, stackTrace) {
    // 4. 异常处理
    appLogger.e('获取数据库日期范围失败，使用默认日期', error: e, stackTrace: stackTrace);
    return _generateDefaultTrainingDate(trainingDays);
  }
}

// 新增：默认日期生成方法
DateTime _generateDefaultTrainingDate(int trainingDays) {
  final now = DateTime.now();
  final defaultMaxDate = DateTime(now.year - 1, now.month, now.day);
  final defaultMinDate = DateTime(now.year - 5, now.month, now.day);
  return _generateRandomDate(defaultMinDate, defaultMaxDate);
}

// 新增：随机日期生成辅助方法
DateTime _generateRandomDate(DateTime start, DateTime end) {
  final random = Random();
  final range = end.difference(start).inDays;
  final randomDays = random.nextInt(range + 1);
  return start.add(Duration(days: randomDays));
}
```

**增强点**：
- ✅ 多层异常处理机制
- ✅ 默认日期生成方法（历史5年范围）
- ✅ 数据量不足时的降级方案
- ✅ 完善的日志追踪

### 9.4 降级策略设计

**降级层级**：
```
1. 最佳情况：使用数据库真实数据范围
   ↓
2. 数据不足：使用可用数据的部分范围
   ↓
3. 数据极少：使用默认历史5年范围
   ↓
4. 异常情况：捕获异常，返回默认日期
```

**默认日期范围**：
- 最小日期：2000-01-01 或 5年前
- 最大日期：当前日期 或 1年前

### 9.5 更新的文件清单

| 文件 | 变更内容 |
|------|----------|
| `lib/data/database/daos/stock_filter_dao.dart` | 增强 getKlineDateRange()，添加数据检查和异常处理 |
| `lib/data/services/time_range_service.dart` | 增强 generateRandomTrainingStartDate()，添加降级机制 |
| `docs/开发记录/训练时间生成失败问题修复记录.md` | 新增：问题修复详细记录 |

## 10. 文档版本

| 版本 | 日期 | 更新内容 | 作者 |
|:---:|------|--------|------|
| v1.0 | 2026-05-18 | 初始版本，创建首页选股条件模块重构技术设计文档 | AI助手 |
| v1.1 | 2026-05-20 | 新增训练时间生成失败修复设计（第9章） | AI助手 |

---

**最后更新**: 2026-05-20
