# K线训练营App - 测试文档

**文档版本**: v1.0  
**最后更新**: 2026-05-13

## 1. 概述

本文档描述 K线训练营App 的测试策略、测试用例和测试流程。

## 2. 测试策略

### 2.1 测试类型

| 测试类型 | 描述 | 优先级 |
|---------|------|--------|
| 单元测试 | 测试单个函数、方法或类的行为 | 高 |
| Widget测试 | 测试Flutter Widget的UI交互和渲染 | 高 |
| 集成测试 | 测试多个组件或模块之间的交互 | 中 |
| 端到端测试 | 模拟用户真实操作流程 | 中 |

### 2.2 测试覆盖率目标

- 单元测试覆盖率：≥ 70%
- Widget测试覆盖率：≥ 50%
- 核心业务逻辑：100% 覆盖

## 3. 测试文件结构

```
test/
├── unit/                  # 单元测试
│   ├── providers/         # Provider测试
│   │   ├── auth_provider_test.dart
│   │   ├── kline_provider_test.dart
│   │   └── trading_provider_test.dart
│   ├── utils/             # 工具函数测试
│   │   └── storage_utils_test.dart
│   └── repositories/      # 仓库测试
│       ├── kline_repository_test.dart
│       └── trading_repository_test.dart
├── widget/                # Widget测试
│   ├── kline_chart_test.dart
│   ├── training_card_test.dart
│   └── trading_button_test.dart
└── integration/           # 集成测试
    ├── training_flow_test.dart
    └── trading_flow_test.dart
```

## 4. 单元测试用例

### 4.1 K线数据Provider测试

**测试文件**: `test/unit/providers/kline_provider_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 获取K线数据成功 | 调用fetchKlineData返回成功 | 返回K线数据列表 |
| 获取K线数据失败 | API返回错误 | 抛出NetworkException |
| K线数据为空 | API返回空列表 | 显示空状态 |
| 切换时间周期 | 从日K切换到周K | 重新获取周K数据 |

### 4.2 交易Provider测试

**测试文件**: `test/unit/providers/trading_provider_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 买入操作 | 执行买入交易 | 持仓增加，资金减少 |
| 卖出操作 | 执行卖出交易 | 持仓减少，资金增加 |
| 买入时资金不足 | 可用资金小于买入金额 | 抛出InsufficientFundsException |
| 卖出时持仓不足 | 卖出数量大于持仓数量 | 抛出InsufficientPositionException |
| 设置止盈单 | 创建止盈条件单 | 条件单列表增加 |
| 设置止损单 | 创建止损条件单 | 条件单列表增加 |
| 触发止盈 | 价格达到止盈价 | 自动卖出，条件单移除 |
| 触发止损 | 价格达到止损价 | 自动卖出，条件单移除 |

### 4.3 工具函数测试

**测试文件**: `test/unit/utils/storage_utils_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 存储字符串 | 保存字符串到SharedPreferences | 成功保存并返回true |
| 获取字符串 | 读取已保存的字符串 | 返回保存的值 |
| 存储整数 | 保存整数到SharedPreferences | 成功保存并返回true |
| 获取整数 | 读取已保存的整数 | 返回保存的值 |
| 获取不存在的键 | 读取未保存的键 | 返回null |
| 清除存储 | 清除所有存储数据 | 所有数据被清除 |

## 5. Widget测试用例

### 5.1 K线图表Widget测试

**测试文件**: `test/widget/kline_chart_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 渲染蜡烛图 | 传入K线数据 | 正确渲染蜡烛图 |
| 显示均线 | 启用MA5/MA10 | 显示均线线条 |
| 隐藏均线 | 禁用均线 | 不显示均线线条 |
| 显示成交量 | 启用成交量显示 | 显示成交量柱状图 |
| 切换时间周期 | 点击周期按钮 | 触发周期切换事件 |
| 缩放图表 | 双指缩放 | 图表缩放正确 |

### 5.2 训练卡片Widget测试

**测试文件**: `test/widget/training_card_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 显示训练信息 | 传入训练数据 | 正确显示股票名称、盈亏等信息 |
| 盈利状态显示 | 盈利为正 | 显示绿色盈利金额 |
| 亏损状态显示 | 盈利为负 | 显示红色亏损金额 |
| 点击详情按钮 | 点击详情 | 触发详情点击事件 |
| 点击复盘按钮 | 点击复盘 | 触发复盘点击事件 |

## 6. 集成测试用例

### 6.1 训练流程测试

**测试文件**: `test/integration/training_flow_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 完整训练流程 | 选择标的→开始训练→交易→结束 | 成功完成训练并生成报告 |
| 条件单触发流程 | 设置止盈→价格达到→自动卖出 | 止盈单正确触发 |
| 网格交易流程 | 设置网格→价格波动→自动交易 | 网格交易正确执行 |

### 6.2 交易流程测试

**测试文件**：`test/integration/trading_flow_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 买入卖出流程 | 买入→持有→卖出 | 持仓和资金正确变化 |
| 多次交易 | 连续多次买卖 | 每次交易记录正确 |
| 资金耗尽 | 持续买入直到资金不足 | 最后一次买入失败 |

## 7. 新增功能测试用例

### 7.1 指标计算测试

**测试文件**：`test/unit/utils/indicator_calculator_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| CCI计算 | 传入K线数据计算CCI | 返回正确CCI值 |
| OBV计算 | 传入K线数据计算OBV | 返回正确OBV值 |
| DMI计算 | 传入K线数据计算DMI | 返回正确+DI/-DI/ADX值 |
| DMA计算 | 传入K线数据计算DMA | 返回正确DMA/AMA值 |
| BBI计算 | 传入K线数据计算BBI | 返回正确BBI值 |

### 7.2 训练流程测试

**测试文件**：`test/integration/training_flow_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 完整训练流程 | 选择标的→开始训练→交易→结束 | 成功完成训练并生成报告 |
| 条件单触发流程 | 设置止盈→价格达到→自动卖出 | 止盈单正确触发 |
| 网格交易流程 | 设置网格→价格波动→自动交易 | 网格交易正确执行 |
| 训练周期显示 | 点击下一步 | 周期显示从1/60变化到2/60 |
| 训练结束对话框 | 到达最后一天 | 自动弹出结果对话框 |
| 训练记录保存 | 完成训练 | 数据保存到training_sessions表 |
| 交易记录保存 | 执行买入/卖出 | 数据保存到trades表 |

### 7.3 训练历史页面测试

**测试文件**：`test/widget/training_history_screen_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 显示训练记录列表 | 有训练记录 | 显示股票名称、代码、收益率等 |
| 空状态显示 | 无训练记录 | 显示"暂无训练记录" |
| 点击跳转详情 | 点击训练卡片 | 跳转到训练详情页面 |
| 显示股票名称 | 有训练记录 | 正确显示股票名称和代码 |

### 7.4 训练详情页面测试

**测试文件**：`test/widget/training_detail_screen_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 显示训练信息 | 传入sessionId | 显示股票、收益率、周期等 |
| 显示交易记录 | 有交易记录 | 显示买入/卖出详情 |
| 买入样式区分 | 买入记录 | 显示红色标识 |
| 卖出样式区分 | 卖出记录 | 显示绿色标识 |

### 7.5 K线滑动缩放测试

**测试文件**：`test/widget/kline_chart_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 双指捏合放大 | 双指捏合操作 | K线图表放大 |
| 双指展开缩小 | 双指展开操作 | K线图表缩小 |
| 单指拖动滑动 | 单指左右拖动 | K线时间轴平移 |
| +按钮放大 | 点击+按钮 | K线放大10% |
| -按钮缩小 | 点击-按钮 | K线缩小10% |
| 最大放大限制 | 放大到极限 | 不再放大 |
| 最小缩小限制 | 缩小到极限 | 不再缩小 |
| 缩放比例显示 | 缩放操作 | 正确显示当前比例 |

### 7.6 指标同步联动测试

**测试文件**：`test/widget/indicator_chart_test.dart`

| 测试用例 | 描述 | 预期结果 |
|---------|------|---------|
| 指标数据范围同步 | K线滑动 | 指标数据范围随可视区间调整 |
| 时间轴对齐 | K线滑动 | 指标时间轴与K线完全对齐 |
| 视觉缩放同步 | K线缩放 | 指标数值显示同步缩放 |
| CCI指标联动 | 滑动/缩放K线 | CCI图表同步变化 |
| OBV指标联动 | 滑动/缩放K线 | OBV图表同步变化 |
| MACD指标联动 | 滑动/缩放K线 | MACD图表同步变化 |
| 多指标同步 | 滑动/缩放K线 | 所有指标图表同步变化 |

## 8. 测试执行命令

```bash
# 运行所有测试
flutter test

# 运行单元测试
flutter test test/unit/

# 运行Widget测试
flutter test test/widget/

# 运行集成测试
flutter test test/integration/

# 生成测试覆盖率报告
flutter test --coverage
genhtml coverage/lcov.info -o coverage/html

# 运行特定测试文件
flutter test test/unit/providers/kline_provider_test.dart
```

## 8. CI/CD集成

### 8.1 GitHub Actions配置

```yaml
name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.x'
          
      - name: Install dependencies
        run: flutter pub get
        
      - name: Run tests
        run: flutter test
        
      - name: Generate coverage
        run: flutter test --coverage
        
      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: coverage/lcov.info
```

### 8.2 测试通过条件

- 所有测试用例通过
- 测试覆盖率 ≥ 70%
- 无编译错误

## 7. K线交互优化测试

### 7.1 K线可视区域优化测试

**测试用例**:

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|-------|
| TC-KLINE-001 | 初始化可视区域 | 1. 进入实战页面<br>2. 观察K线显示 | 当前训练日显示在K线图最右侧 | 高 |
| TC-KLINE-002 | 点击下一步自动滑动 | 1. 进入实战页面<br>2. 点击下一步按钮 | K线向左滑动，新K线从右侧进入 | 高 |
| TC-KLINE-003 | 当前日始终可视 | 1. 连续点击下一步<br>2. 观察当前日位置 | 当前训练日始终在可视范围内 | 高 |
| TC-KLINE-004 | 右滑边界限制 | 1. 滑动K线到最右侧<br>2. 继续右滑 | 不会滑出空白区域 | 高 |

### 7.2 买卖点位实时显示测试

**测试用例**:

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|-------|
| TC-TRADE-001 | 买入标记显示 | 1. 进入实战页面<br>2. 点击买入按钮<br>3. 确认买入 | 当前K线上显示红色买入标记 | 高 |
| TC-TRADE-002 | 卖出标记显示 | 1. 买入股票<br>2. 点击卖出按钮<br>3. 确认卖出 | 当前K线上显示绿色卖出标记 | 高 |
| TC-TRADE-003 | 标记随K线滑动 | 1. 买入股票<br>2. 向左滑动K线 | 买入标记随对应K线移动 | 高 |
| TC-TRADE-004 | 标记索引映射 | 1. 多次买卖<br>2. 滑动K线 | 所有标记显示位置准确 | 高 |

### 7.3 滑动缩放测试

**测试用例**:

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|-------|
| TC-ZOOM-001 | +按钮放大 | 1. 进入实战页面<br>2. 点击+按钮 | K线放大，显示更少K线 | 高 |
| TC-ZOOM-002 | -按钮缩小 | 1. 进入实战页面<br>2. 点击-按钮 | K线缩小，显示更多K线 | 高 |
| TC-ZOOM-003 | 左箭头滑动 | 1. 进入实战页面<br>2. 点击左箭头 | K线向左滑动 | 高 |
| TC-ZOOM-004 | 右箭头滑动 | 1. 进入实战页面<br>2. 点击右箭头 | K线向右滑动 | 高 |
| TC-ZOOM-005 | 双指缩放 | 1. 进入实战页面<br>2. 双指捏合/展开 | K线放大/缩小 | 中 |
| TC-ZOOM-006 | 单指滑动 | 1. 进入实战页面<br>2. 单指左右拖动 | K线左右滑动 | 中 |

### 7.4 指标同步联动测试

**测试用例**:

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
|---------|---------|---------|---------|-------|
| TC-INDICATOR-001 | 成交量同步 | 1. 滑动K线<br>2. 观察成交量图表 | 成交量数据随K线同步变化 | 高 |
| TC-INDICATOR-002 | MACD同步 | 1. 滑动K线<br>2. 观察MACD图表 | MACD数据随K线同步变化 | 高 |
| TC-INDICATOR-003 | 时间轴对齐 | 1. 滑动K线<br>2. 对比指标图表 | 指标时间轴与K线完全对齐 | 高 |
| TC-INDICATOR-004 | 缩放同步 | 1. 缩放K线<br>2. 观察指标图表 | 指标显示范围随缩放调整 | 中 |

---

**文档版本**: v1.2  
**生成日期**: 2026-05-13  
**最后更新**: 2026-05-18