# 实战页面增强功能开发记录

## 开发时间
2026-05-16

## 开发概述
根据对话需求，对K线训练营APP的实战页面进行增强，主要包括：
- K线平滑渲染优化
- 买卖点位标记
- 时间步进器和下一步按钮
- 换股、条件单、买入、卖出功能
- 买入/卖出独立弹窗页面
- 首页确认按钮

---

## 一、需求分析

### 1.1 原始需求
```
实战页面的K线展示，需要更适配机器，k线走势需要更加顺滑，同时需要展示买入卖出的点位，在换股、条件单、买入、卖出的模块，新增"下一步"按钮，通过点击下一步，实现K线每天的变化。k线的变化需要从数据库读取真实数据，展示到页面中
```

### 1.2 需求细化
根据对话确认，进一步明确：
- **下一步按钮**：点击后K线推进一天，从数据库加载真实数据
- **换股**：弹出选股页面，切换标的
- **条件单**：弹出条件单设置页面
- **买入**：弹出买入页面，支持价格/数量/仓位选项
- **卖出**：弹出卖出页面，支持价格/数量/仓位选项

### 1.3 补充需求
```原文
1. 在首页选股条件新增确认按钮，选择好条件后，点击确认跳转到实战页面
2. 实战页面的图片对应的模块需要删除
3. 实战页面的下一步按钮之前展示选股条件中选择的时间周期对应的时长（例如20天、60天）
4. 点击买入按钮，弹出新的页面，支持选择买入价格、买入股数，同时有1/2仓、1/3仓、全仓等默认选项，点击确认后完成买入，点击取消按钮返回到原页面
5. 点击卖出按钮，功能与买入类似
```

---

## 二、实现内容

### 2.1 新增/修改文件
| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `lib/features/training/widgets/kline_chart.dart` | 修改 | 新增TradePoint类、平滑均线、买卖点位标记 |
| `lib/features/home/home_screen.dart` | 修改 | 新增确认按钮 |
| `lib/features/battle/battle_screen.dart` | 重写 | 新增所有功能 |
| `lib/data/repositories/kline_repository.dart` | 修改 | 数据库优先读取 |
| `specs/产品概述.md` | 修改 | 更新功能描述 |
| `docs/requirements/需求总览文档.md` | 新增 | 需求总览 |
| `docs/features/功能详细说明文档.md` | 新增 | 功能详细说明 |
| `docs/tech/技术说明文档.md` | 新增 | 技术实现说明 |
| `docs/test/测试说明文档.md` | 新增 | 测试用例说明 |

### 2.2 核心功能实现

#### 2.2.1 平滑K线走势
**技术实现**：二阶贝塞尔曲线
```dart
void _drawSmoothLine(Canvas canvas, Size size, List<double> values, Color color) {
  // 使用 quadraticBezierTo 实现平滑曲线
  path.quadraticBezierTo(prevX, prevY, cpX, (prevY + y) / 2);
}
```

#### 2.2.2 买卖点位标记
**数据结构**：
```dart
class TradePoint {
  final int index;       // K线索引
  final double price;    // 交易价格
  final bool isBuy;      // true=买入，false=卖出
  final String label;    // 显示标签
  final DateTime date;   // 交易日期
}
```

#### 2.2.3 买入/卖出弹窗
**核心组件**：`_TradeDialog`
- 支持价格输入（默认当前价）
- 支持数量输入
- 提供1/3仓、1/2仓、全仓快捷按钮
- 实时显示金额预览
- 卖出时显示预计盈亏

#### 2.2.4 数据库优先读取
```dart
Future<List<KlineModel>> fetchKlineData(...) async {
  // 1. 优先从数据库读取
  final dbData = await fetchKlineDataFromDb(...);
  if (dbData.isNotEmpty) return dbData;

  // 2. 数据库没有从API获取
  final apiData = await _api.fetchKlineData(...);
  if (apiData.isNotEmpty) {
    await _saveToDatabase(apiData, timeframe);
    return apiData;
  }

  // 3. 兜底使用模拟数据
  return _generateMockData(symbol, limit);
}
```

---

## 三、修复的问题

### 3.1 编译错误修复
| 问题 | 修复 |
|------|------|
| `const Text('${widget.title}金额:')` | 移除const关键字 |
| `paddingBottom: MediaQuery.of(context).viewInsets.bottom + 16` | 改用 `EdgeInsets.fromLTRB()` |

---

## 四、测试验证

### 4.1 测试环境
- Flutter SDK: 3.x
- 目标设备: iPhone 17 Pro (iOS Simulator)
- 构建命令: `flutter run -d "0D4B29EB-252F-4858-8704-C5B644EF5EA7" --debug`

### 4.2 验证结果
| 功能 | 验证结果 |
|------|:---:|
| 首页确认按钮跳转 | ✅ |
| 实战页面K线显示 | ✅ |
| 平滑均线渲染 | ✅ |
| 买卖点位标记 | ✅ |
| 时间步进器 | ✅ |
| 下一步按钮 | ✅ |
| 训练周期显示 | ✅ |
| 买入弹窗 | ✅ |
| 卖出弹窗 | ✅ |
| 换股功能 | ✅ |
| 条件单功能 | ✅ |

---

## 五、文档产出

### 5.1 文档结构
```
docs/
├── requirements/
│   ├── 需求总览文档.md          # 需求总览
│   └── 需求说明文档.md          # "我的"页面需求
├── features/
│   └── 功能详细说明文档.md      # 功能详细说明
├── tech/
│   └── 技术说明文档.md          # 技术实现说明
└── test/
    └── 测试说明文档.md          # 测试用例说明
```

### 5.2 后续文档更新机制
后续新增需求时，自动按以下规则更新：
1. **需求总览文档**：新增需求条目，分配需求编号
2. **功能详细说明文档**：新增功能模块说明
3. **技术说明文档**：新增技术实现说明
4. **测试说明文档**：新增测试用例

---

## 六、下一步计划

### 6.1 待开发功能
- [ ] 我的模块完整实现
- [ ] 用户认证（注册/登录）
- [ ] 训练记录持久化
- [ ] 条件单自动触发逻辑

### 6.2 优化方向
- [ ] K线渲染性能优化
- [ ] 支持更多技术指标
- [ ] 添加策略回测功能

---

## 七、版本信息

| 版本 | 日期 | 更新内容 |
|:---:|------|--------|
| v1.0 | 2026-05-16 | 初始版本，完成实战页面增强功能 |

---

**记录人**: AI助手
**最后更新**: 2026-05-16