# 训练时间范围生成失败问题修复记录

**开发日期**: 2026-05-18
**版本**: v1.7
**开发者**: Claude AI

---

## 问题描述

用户反馈：训练时间范围生成失败，页面在转圈，无法生成训练日期。

## 问题分析

### 错误日志

```
FormatException: Invalid radix-10 number (at character 1) 2005-01-04
```

### 根本原因

数据库中的 `trade_date` 字段存储为字符串格式（如 "2005-01-04"），但 Drift ORM 模型中定义为 `DateTimeColumn`，导致 Drift 尝试将字符串解析为整数时间戳，抛出 `FormatException`。

## 解决方案

### 1. 修改日期查询逻辑

在所有日期相关的查询中，使用自定义 SQL 查询和手动日期转换，而不是依赖 Drift 的自动映射。

### 2. 添加日期转换辅助方法

```dart
/// 日期转字符串格式 "YYYY-MM-DD"
String _dateToString(DateTime date) {
  return '${date.year}-${date.month.toString().padLeft(2, '0')}-${date.day.toString().padLeft(2, '0')}';
}

/// 字符串转日期 "YYYY-MM-DD"
DateTime _stringToDate(String dateStr) {
  try {
    final parts = dateStr.split('-');
    if (parts.length == 3) {
      return DateTime(int.parse(parts[0]), int.parse(parts[1]), int.parse(parts[2]));
    }
  } catch (e) {
    // 如果解析失败，返回默认日期
  }
  return DateTime(2000, 1, 1);
}
```

### 3. 使用自定义SQL查询

```dart
Future<(DateTime, DateTime)> getKlineDateRange() async {
  appLogger.i('开始查询K线数据日期范围...');
  
  // 使用自定义SQL查询，因为数据库中存储的是日期字符串
  final minResult = await customSelect(
    'SELECT MIN(trade_date) as min_date FROM kline_data WHERE period = ?',
    variables: [const Variable('day')],
  ).getSingle();
  
  final maxResult = await customSelect(
    'SELECT MAX(trade_date) as max_date FROM kline_data WHERE period = ?',
    variables: [const Variable('day')],
  ).getSingle();

  final minDateStr = minResult.read<String?>('min_date');
  final maxDateStr = maxResult.read<String?>('max_date');
  
  appLogger.i('查询到的日期字符串: min=$minDateStr, max=$maxDateStr');

  return (
    minDateStr != null ? _stringToDate(minDateStr) : DateTime(2000, 1, 1),
    maxDateStr != null ? _stringToDate(maxDateStr) : DateTime.now(),
  );
}
```

## 修改的文件

### 1. `lib/data/database/daos/stock_filter_dao.dart`

- 添加 `_dateToString()` 和 `_stringToDate()` 辅助方法
- 修改 `getKlineDateRange()` 使用自定义SQL查询
- 修改 `getLatestKlineDate()` 使用自定义SQL查询
- 修改 `getKlineDataForDate()` 使用自定义SQL查询
- 修改 `getLastKlineDataInRange()` 使用自定义SQL查询
- 修改 `getKlineDataBefore()` 使用自定义SQL查询
- 修改 `_getHistoricalMaxClose()` 使用自定义SQL查询
- 修改 `_getHistoricalMinClose()` 使用自定义SQL查询
- 添加 `_buildKlineDataFromRow()` 手动构建KlineDataData对象

### 2. `lib/data/database/daos/kline_dao.dart`

- 添加 `_dateToString()` 和 `_stringToDate()` 辅助方法
- 修改 `getKlineData()` 使用自定义SQL查询
- 修改 `getKlineDataRange()` 使用自定义SQL查询
- 添加 `_buildKlineDataFromRow()` 手动构建KlineDataData对象

## 测试验证

### 测试步骤

1. 启动应用，进入首页
2. 观察训练日期是否自动生成
3. 检查日志输出

### 测试结果

✅ **成功**：训练日期成功生成

**日志输出**:
```
💡 开始生成训练日期...
💡 开始生成训练起始日期...
💡 开始查询K线数据日期范围...
💡 查询到的日期字符串: min=2005-01-04, max=2026-05-18
💡 数据库日期范围: min=2005-01-04 00:00:00.000, max=2026-05-18 00:00:00.000
💡 有效日期范围: start=2005-10-31 00:00:00.000, end=2025-12-19 00:00:00.000
💡 可选天数范围: 7354天
💡 生成的训练起始日期: 2023-11-20 00:00:00.000
💡 成功生成训练日期: 2023-11-20 00:00:00.000
```

## 影响范围

- 训练日期生成功能
- 所有日期相关的数据库查询
- 选股条件筛选功能

## 后续优化建议

1. **数据库迁移**：考虑将数据库中的日期字段从字符串格式迁移为整数时间戳格式，以匹配 Drift 的默认行为
2. **TypeConverter**：创建自定义 TypeConverter 来统一处理日期转换逻辑
3. **单元测试**：添加日期转换的单元测试，确保边界情况处理正确

---

**状态**: ✅ 已完成
**测试**: ✅ 已验证
**文档更新**: ✅ 已更新
