# 选股功能优化开发记录

**开发日期**: 2026-05-16  
**需求ID**: REQ-010, REQ-011, REQ-012  
**功能模块**: 选股功能  
**版本**: v1.5

---

## 需求背景

1. **REQ-010**: 用户选择时间范围（如2015-2016年）后，选股结果始终为0
2. **REQ-011**: 选择"近1年"、"近3年"等预置时间范围后，需要显示具体日期
3. **REQ-012**: 市场选择从单选改为多选，支持独立选择上证/深证

---

## 问题分析

### REQ-010: 选股结果为0的原因

**现象**: 
- 数据库中有2015-2016年的数据（558只股票，248,271条K线）
- 但选择自定义时间范围后选股结果为0

**根本原因**:
1. 原算法使用 `getKlineDataForDate(symbol, 'day', date)` 精确查找指定日期的数据
2. 当传入的date为"今天"(2026-05-16)，但数据库最新数据是2026-05-11时，返回null
3. 上升趋势等算法需要55天历史数据+前一天数据，数据不连续导致筛选失败

**算法调用链**:
```
filterStocks()
  → filterByCondition()
    → checkUpTrend()
      → getKlineDataBefore() 获取历史数据
      → calculateMA() 计算均线
      → 如果数据不足55天，返回false
```

---

## 实现方案

### 1. 新增时间范围内最后一条数据查询方法

**文件**: `lib/data/database/daos/stock_filter_dao.dart`

```dart
Future<KlineDataData?> getLastKlineDataInRange(
  String symbol, 
  String period, 
  DateTime startDate, 
  DateTime endDate,
) async {
  return (select(klineData)
        ..where((t) => t.symbol.equals(symbol))
        ..where((t) => t.period.equals(period))
        ..where((t) => t.tradeDate.isBiggerOrEqualValue(startDate))
        ..where((t) => t.tradeDate.isSmallerOrEqualValue(endDate))
        ..orderBy([(t) => OrderingTerm.desc(t.tradeDate)])
        ..limit(1))
      .getSingleOrNull();
}
```

### 2. 修改选股Repository支持时间范围

**文件**: `lib/data/repositories/stock_filter_repository.dart`

```dart
Future<List<StockFilterResultModel>> _getStockDetails(
  List<String> symbols,
  DateTime date, {
  DateTime? startDate,
  DateTime? endDate,
}) async {
  // ...
  KlineDataData? klineData;
  if (startDate != null && endDate != null) {
    klineData = await _stockFilterDao.getLastKlineDataInRange(
      symbolCode,
      'day',
      startDate,
      endDate,
    );
  } else {
    klineData = await _stockFilterDao.getKlineDataForDate(
      symbolCode,
      'day',
      date,
    );
  }
  // ...
}
```

### 3. 时间周期显示具体日期

**文件**: `lib/core/enums/stock_filter_condition.dart`

```dart
String get displayLabel {
  String formatDate(DateTime date) {
    return '${date.year}-${date.month.toString().padLeft(2, '0')}-${date.day.toString().padLeft(2, '0')}';
  }
  
  switch (type) {
    case TimeRangeType.recent1Year:
    case TimeRangeType.recent3Years:
    case TimeRangeType.recent5Years:
      if (startDate != null && endDate != null) {
        return '${formatDate(startDate!)} ~ ${formatDate(endDate!)}';
      }
      return type.label;
    case TimeRangeType.custom:
      if (startDate != null && endDate != null) {
        return '${formatDate(startDate!)} ~ ${formatDate(endDate!)}';
      }
      return '自定义';
  }
}
```

### 4. 市场多选功能

**文件**: `lib/providers/stock_filter_provider.dart`

```dart
class StockFilterState {
  // 新增字段
  final List<String> selectedMarkets;
  // ...
}

class StockFilterNotifier extends StateNotifier<StockFilterState> {
  void toggleMarket(String marketCode) {
    final current = List<String>.from(state.selectedMarkets);
    if (current.contains(marketCode)) {
      current.remove(marketCode);
    } else {
      current.add(marketCode);
    }
    state = state.copyWith(selectedMarkets: current);
    if (state.selectedCondition != null && ...) {
      executeFilter();
    }
  }

  void selectAllMarkets() {
    state = state.copyWith(selectedMarkets: ['XSHG', 'XSHE']);
    // ...
  }

  void clearMarkets() {
    state = state.copyWith(selectedMarkets: []);
  }
}
```

---

## UI变更

### 时间范围选择器

**位置**: `lib/features/home/widgets/stock_condition_selector.dart`

```dart
Widget _buildTimeRangeSelector() {
  final timeRange = widget.selectedTimeRange ?? StockTimeRange.recent1Year();
  
  return Column(
    children: [
      Row(
        children: [
          // ...
          // 新增：显示具体日期范围
          Container(
            padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
            decoration: BoxDecoration(
              color: AppTheme.accent.withAlpha(25),
              borderRadius: BorderRadius.circular(4),
            ),
            child: Text(
              timeRange.displayLabel,  // 如: 2025-05-16 ~ 2026-05-16
              style: const TextStyle(
                fontSize: 11,
                color: AppTheme.accent,
              ),
            ),
          ),
        ],
      ),
      // 时间范围chips...
    ],
  );
}
```

### 市场多选组件

```dart
Widget _buildMarketSelector() {
  final selectedMarkets = ref.watch(stockFilterProvider).selectedMarkets;
  
  return Column(
    children: [
      Row(children: [/* 市场选择标题 */]),
      Wrap(
        spacing: 8,
        children: [
          _MarketChip(
            label: '上证',
            isSelected: selectedMarkets.isEmpty || selectedMarkets.contains('XSHG'),
            onTap: () => ref.read(stockFilterProvider.notifier).toggleMarket('XSHG'),
          ),
          _MarketChip(
            label: '深证',
            isSelected: selectedMarkets.isEmpty || selectedMarkets.contains('XSHE'),
            onTap: () => ref.read(stockFilterProvider.notifier).toggleMarket('XSHE'),
          ),
        ],
      ),
    ],
  );
}
```

---

## 相关文件变更

### 修改的文件
| 文件 | 变更内容 |
|------|----------|
| `lib/data/database/daos/stock_filter_dao.dart` | 新增 `getLastKlineDataInRange()` 方法 |
| `lib/data/repositories/stock_filter_repository.dart` | 修改 `_getStockDetails()` 支持时间范围 |
| `lib/core/enums/stock_filter_condition.dart` | 修改 `displayLabel` 显示具体日期 |
| `lib/providers/stock_filter_provider.dart` | 新增市场多选状态管理 |
| `lib/features/home/widgets/stock_condition_selector.dart` | 新增市场多选UI |

### 新增的文件
| 文件 | 说明 |
|------|------|
| `docs/开发记录/选股功能优化开发记录.md` | 本文档 |

---

## 测试验证

### 本地测试
```bash
# 验证数据库数据
sqlite3 lib/data/database/kline_trainer.db "
  SELECT COUNT(*) FROM kline_data 
  WHERE trade_date >= '2015-01-01' AND trade_date <= '2016-12-31';
  -- 预期: 248,271
"
```

### 模拟器测试
```bash
# 启动应用
flutter run -d "iPhone 17 Pro"
```

**验证项**:
1. 时间范围选择后，标题旁显示具体日期（如 `2025-05-16 ~ 2026-05-16`）
2. 市场多选按钮正常显示，点击可切换选中状态
3. 选择任意选股条件后，能看到筛选结果

---

## 版本记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.5 | 2026-05-16 | 初始实现，选股算法优化、时间周期显示、市场多选 |

---

*开发记录创建日期: 2026-05-16*
*最后更新: 2026-05-16*