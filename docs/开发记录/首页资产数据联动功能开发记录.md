# 首页资产数据联动功能开发记录

## 概述

本文档记录首页训练页面资产信息从数据库获取的实现过程，实现前后端及数据库的联动。

## 开发时间

- 开发日期：2026-05-16
- 功能版本：v1.0

## 需求来源

用户需求：首页的训练页面信息初始资金为10万，现有资产、总盈亏等字段信息需要根据实战的训练情况，从数据库获取。实现前后端及数据库的联动。

## 实现概述

### 新增文件

#### 1. AssetSummaryModel（资产汇总数据模型）
位置：`lib/data/models/asset_summary_model.dart`

```dart
class AssetSummaryModel {
  final double initialCapital;      // 初始资金
  final double currentCapital;      // 当前资产
  final double totalProfit;         // 总盈亏
  final double profitRate;          // 收益率
  final int totalTradeCount;        // 操作次数
  final int totalTradeDays;         // 操作天数
  final int winCount;               // 盈利次数
  final double winRate;             // 成功率
  final double maxProfit;           // 最大盈利
  final double maxLoss;            // 最大亏损
  final double maxDrawdown;        // 最大回撤
  final double annualizedReturn;   // 年化收益
  final double sharpeRatio;        // 夏普比率
  final double profitLossRatio;    // 盈亏比

  const AssetSummaryModel({
    this.initialCapital = 100000.0,
    this.currentCapital = 100000.0,
    // ...其他字段
  });
}
```

#### 2. AssetSummaryProvider（资产状态管理）
位置：`lib/providers/asset_summary_provider.dart`

```dart
final assetSummaryProvider =
    AsyncNotifierProvider<AssetSummaryNotifier, AssetSummaryModel>(
  AssetSummaryNotifier.new,
);

class AssetSummaryNotifier extends AsyncNotifier<AssetSummaryModel> {
  static const int defaultUserId = 1;

  @override
  Future<AssetSummaryModel> build() async {
    return _loadAssetSummary();
  }

  Future<AssetSummaryModel> _loadAssetSummary() async {
    final dbService = DatabaseService.instance;
    final data = await dbService.trainingDao.getUserAssetSummary(defaultUserId);
    // 转换为AssetSummaryModel
  }

  Future<void> refresh() async {
    state = const AsyncValue.loading();
    state = await AsyncValue.guard(() => _loadAssetSummary());
  }
}
```

### 修改文件

#### 1. TrainingDao（新增聚合查询方法）
位置：`lib/data/database/daos/training_dao.dart`

新增方法：
- `getUserAssetSummary(int userId)` - 获取用户资产汇总数据
- `getLatestSession(int userId)` - 获取用户最新会话

#### 2. HomeScreen（更新资产卡片联动数据库）
位置：`lib/features/home/home_screen.dart`

修改内容：
- 引入`assetSummaryProvider`和`AssetSummaryModel`
- 添加`_formatCurrency()`和`_formatPercent()`格式化方法
- 修改`_buildAssetStatsCard()`使用`ref.watch(assetSummaryProvider)`监听数据
- 新增`_buildAssetCardLoading()`加载状态
- 新增`_buildAssetCardContent()`根据汇总数据显示资产信息
- 修改`_buildBattleStats()`联动数据库获取实战统计数据

## 数据流向

```
┌─────────────────────────────────────────────────────────────┐
│                     HomeScreen                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  ref.watch(assetSummaryProvider)                     │   │
│  │    ↓                                                 │   │
│  │  AsyncValue<AssetSummaryModel>                        │   │
│  └──────────────────────────────────────────────────────┘   │
│                           ↓                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  AssetSummaryNotifier.build()                         │   │
│  │    ↓                                                 │   │
│  │  _loadAssetSummary()                                 │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              TrainingDao.getUserAssetSummary()             │
│  1. getUserSessions(userId, limit: 1000)                    │
│  2. 遍历sessions聚合计算:                                   │
│     - 初始资金总和(initialCapital)                          │
│     - 当前资金总和(currentCapital)                          │
│     - 总盈亏(totalProfit)                                  │
│     - 操作天数统计(tradeDays)                               │
│     - 盈利次数(winCount)                                   │
│     - 最大回撤(maxDrawdown)                                │
│  3. 计算年化收益率(annualizedReturn)                        │
│  4. 返回Map<String, dynamic>                               │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    TrainingSessions表                        │
│  ┌────────────────────────────────────────────────────┐    │
│  │  id | initialCapital | currentCapital | totalProfit│    │
│  │  1  | 100000.0      | 105000.0      | 5000.0      │    │
│  │  2  | 100000.0      | 98000.0       | -2000.0     │    │
│  │  3  | 100000.0      | 110000.0      | 10000.0    │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## UI展示字段

### 资产统计卡片
| 字段 | 数据来源 | 默认值 |
|------|----------|--------|
| 初始资产 | 汇总计算 | ¥100,000 |
| 现有资产 | 汇总计算 | ¥100,000 |
| 总盈亏 | 汇总计算 | ¥0 |
| 收益率 | 计算得出 | 0% |
| 操作次数 | 汇总计算 | 0 |
| 操作天数 | 汇总计算 | 0 |
| 盈利次数 | 汇总计算 | 0 |
| 成功率 | 计算得出 | 0% |
| 最大盈利 | 计算得出 | ¥0 |
| 最大亏损 | 计算得出 | ¥0 |
| 最大回撤 | 汇总计算 | 0% |
| 年化收益 | 计算得出 | 0% |

### 实战卡片
| 字段 | 数据来源 | 计算方式 |
|------|----------|----------|
| 操盘评级 | A+/A/B+/B/C+/C | 根据收益率分级 |
| 总收益率 | profitRate | 直接使用 |
| 交易次数 | totalTradeCount | 直接使用 |
| 年化收益率 | annualizedReturn | 直接使用 |
| 本月收益 | totalProfit | 直接使用 |
| 最大回撤 | maxDrawdown | 直接使用 |
| 夏普比率 | sharpeRatio | 直接使用 |
| 盈亏比 | profitLossRatio | 直接使用 |

## 评级计算规则

```dart
String _calculateRating(double profitRate) {
  if (profitRate >= 50) return 'A+';
  if (profitRate >= 30) return 'A';
  if (profitRate >= 20) return 'B+';
  if (profitRate >= 10) return 'B';
  if (profitRate >= 0) return 'C+';
  return 'C';
}
```

## 文件变更清单

### 新增文件
| 文件路径 | 说明 |
|----------|------|
| `lib/data/models/asset_summary_model.dart` | 资产汇总数据模型 |
| `lib/providers/asset_summary_provider.dart` | 资产状态管理Provider |

### 修改文件
| 文件路径 | 修改内容 |
|----------|----------|
| `lib/data/database/daos/training_dao.dart` | 新增getUserAssetSummary和getLatestSession方法 |
| `lib/features/home/home_screen.dart` | 更新资产卡片联动数据库 |

## 测试验证

### 测试场景

1. **首次启动（无训练数据）**
   - 显示默认值：初始资产¥100,000，现资产¥100,000，总盈亏¥0
   - 所有统计字段显示0或默认值

2. **有训练数据后**
   - 从TrainingSessions表聚合数据
   - 正确显示总盈亏、收益率、操作次数等统计

3. **加载状态**
   - 显示加载动画
   - 加载完成后显示数据

## 已知问题

- 无

## 待优化项

- [ ] 添加数据刷新功能（支持下拉刷新）
- [ ] 添加缓存机制避免频繁查询
- [ ] 增加更多统计指标（如月度收益曲线）

---

*文档版本: v1.0*
*创建日期: 2026-05-16*
*更新日期: 2026-05-16*