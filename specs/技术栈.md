# K线训练营App - 技术栈文档

## 一、项目概述

**项目名称**：K线训练营App  
**项目类型**：跨平台金融教育应用  
**支持平台**：iOS、Android、macOS、Windows  
**当前状态**：已完成基础架构搭建，macOS构建成功

## 二、技术栈选择

### 2.1 核心框架

| 分类 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| 框架 | Flutter | 3.41.9 | 跨平台支持，性能优异，社区成熟 |
| 语言 | Dart | 3.2+ | 类型安全，JIT/AOT编译，与Flutter深度集成 |

### 2.2 状态管理

| 技术 | 版本 | 用途 |
|------|------|------|
| Riverpod | 2.6.1 | 轻量高效的状态管理，代码生成支持 |
| GoRouter | 12.1.3 | 声明式路由管理 |

### 2.3 UI组件

| 技术 | 版本 | 用途 |
|------|------|------|
| Flutter Material 3 | - | 现代化UI组件库 |
| fl_chart | 0.65.0 | 轻量级图表库，支持K线展示（替代Syncfusion） |

### 2.4 数据层

| 技术 | 版本 | 用途 |
|------|------|------|
| SQLite | - | 本地数据持久化 |
| drift | 2.28.2 | SQLite ORM |
| Dio | 5.4+ | HTTP客户端 |
| shared_preferences | 2.2.0 | 简单键值存储 |

### 2.5 开发工具

| 工具 | 版本 | 用途 |
|------|------|------|
| Flutter SDK | 3.41.9 | 核心开发工具 |
| Android Studio / VS Code | - | IDE |
| build_runner | 2.5.4 | 代码生成工具 |
| json_serializable | 6.9.5 | JSON序列化代码生成 |

### 2.6 后端服务（可选）

| 服务 | 用途 |
|------|------|
| Firebase Auth | 用户认证 |
| Firebase Firestore | 云端数据存储 |
| Firebase Cloud Functions | 后端逻辑 |
| WebSocket | 实时数据推送 |

## 三、环境要求

- **Flutter SDK**: >= 3.41.0
- **Dart SDK**: >= 3.2.0
- **Android Studio**: >= Hedgehog
- **Xcode**: >= 15.0 (macOS/iOS)
- **CocoaPods**: >= 1.16.0 (macOS/iOS)
- **Node.js**: >= 18.0 (用于构建工具)

## 四、依赖清单

```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # 状态管理
  flutter_riverpod: ^2.3.0
  riverpod_annotation: ^2.1.0
  
  # 路由
  go_router: ^12.0.0
  
  # 图表
  fl_chart: ^0.65.0
  
  # 网络
  dio: ^5.4.0
  
  # 数据库
  drift: ^2.11.0
  sqlite3_flutter_libs: ^0.5.0
  
  # 工具
  shared_preferences: ^2.2.0
  flutter_dotenv: ^5.1.0
  intl: ^0.19.0
  json_annotation: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  riverpod_generator: ^2.2.0
  drift_dev: ^2.11.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
```

## 五、技术决策记录

### 5.1 图表库替换说明

| 原方案 | 当前方案 | 替换原因 |
|--------|----------|----------|
| syncfusion_flutter_charts | fl_chart | Syncfusion与Flutter 3.41存在API不兼容问题，fl_chart更轻量且兼容性更好 |

### 5.2 ThemeData API适配

由于Flutter 3.17+ 对ThemeData API进行了调整：
- `accentColor` → `colorScheme.secondary`
- `backgroundColor` → `colorScheme.background`
- `FontWeight.semiBold` → `FontWeight.w600`

## 六、运行注意事项

### macOS构建

首次运行macOS需要：
1. 添加macOS平台支持：`flutter create . --platforms macos`
2. 安装CocoaPods：`brew install cocoapods`
3. 禁用GPU着色器编译（解决编译问题）：
   ```bash
   FLUTTER_DISABLE_GPU_SHADERS=true flutter run -d macos --release
   ```
